#!/bin/bash
REUSSITE=0
ECHEC=0

for  MOT in `java -jar $1 pourcen/03.04m.jpg`
do
    if [ "$MOT" = "rsain" ]
then
REUSSITE=$(($REUSSITE+1))

else 
ECHEC=$(($ECHEC+1))

fi

done 

for  MOT in `java -jar $1 pourcen/03.04s.jpg`
do
    if [ "$MOT" = "rsain" ]
then
REUSSITE=$(($REUSSITE+1))

else 
ECHEC=$(($ECHEC+1))

fi

done 

for  MOT in `java -jar $1 pourcen/04.04m.png`
do
    if [ "$MOT" = "rsain" ]
then
REUSSITE=$(($REUSSITE+1))

else 
ECHEC=$(($ECHEC+1))

fi

done 

for  MOT in `java -jar $1 pourcen/04.04s.png`
do
    if [ "$MOT" = "rsain" ]
then
REUSSITE=$(($REUSSITE+1))

else 
ECHEC=$(($ECHEC+1))

fi

done 

for  MOT in `java -jar $1 pourcen/05.04s.png`
do
    if [ "$MOT" = "rsain" ]
then
REUSSITE=$(($REUSSITE+1))

else 
ECHEC=$(($ECHEC+1))

fi

done 

for  MOT in `java -jar $1 pourcen/06.04m.png`
do
    if [ "$MOT" = "rsain" ]
then
REUSSITE=$(($REUSSITE+1))

else 
ECHEC=$(($ECHEC+1))

fi

done 

for  MOT in `java -jar $1 pourcen/07.04.png`
do
    if [ "$MOT" = "rsain" ]
then
REUSSITE=$(($REUSSITE+1))

else 
ECHEC=$(($ECHEC+1))

fi

done 

for  MOT in `java -jar $1 pourcen/07.04s.png`
do
    if [ "$MOT" = "rsain" ]
then
REUSSITE=$(($REUSSITE+1))

else 
ECHEC=$(($ECHEC+1))

fi

done 

for  MOT in `java -jar $1 pourcen/08.04m.png`
do
    if [ "$MOT" = "rsain" ]
then
REUSSITE=$(($REUSSITE+1))

else 
ECHEC=$(($ECHEC+1))

fi

done 

for  MOT in `java -jar $1 08.04s.png`
do
    if [ "$MOT" = "rsain" ]
then
REUSSITE=$(($REUSSITE+1))

else 
ECHEC=$(($ECHEC+1))

fi

done 

for  MOT in `java -jar $1 09.04m.png`
do
    if [ "$MOT" = "rsain" ]
then
REUSSITE=$(($REUSSITE+1))

else 
ECHEC=$(($ECHEC+1))

fi

done 

for  MOT in `java -jar $1 pourcen/09.04s.png`
do
    if [ "$MOT" = "rsain" ]
then
REUSSITE=$(($REUSSITE+1))

else 
ECHEC=$(($ECHEC+1))

fi

done 

for  MOT in `java -jar $1 pourcen/10.04m.png`
do
    if [ "$MOT" = "rsain" ]
then
REUSSITE=$(($REUSSITE+1))

else 
ECHEC=$(($ECHEC+1))

fi

done 

for  MOT in `java -jar $1 pourcen/10.04s.png`
do
    if [ "$MOT" = "rsain" ]
then
REUSSITE=$(($REUSSITE+1))

else 
ECHEC=$(($ECHEC+1))

fi

done 





echo "REUSSITE = $REUSSITE"
